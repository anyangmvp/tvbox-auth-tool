<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.TVTools" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/neon_cyan</item>
        <item name="colorPrimaryDark">@color/cyber_dark</item>
        <item name="colorAccent">@color/neon_magenta</item>
        <item name="android:windowBackground">#0D0D0D</item>
        <item name="android:statusBarColor">#0D0D0D</item>
    </style>
</resources>